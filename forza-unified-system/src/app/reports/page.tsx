import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function ReportsPage() {
  return (
    <ModulePlaceholder
      title="Reports"
      description="Complete analytical reports with metrics, tables, bar charts, pie charts, line charts, PDF export, CSV export, header, and footer."
    />
  );
}
import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function RecipeMakerPage() {
  return (
    <ModulePlaceholder
      title="Recipe Maker"
      description="Recipe creation, recipe costing, ingredient sync, batch yield, portion yield, cost per portion, selling price, and margin calculations."
    />
  );
}
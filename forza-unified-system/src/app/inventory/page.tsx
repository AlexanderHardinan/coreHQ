import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function InventoryPage() {
  return (
    <ModulePlaceholder
      title="Inventory"
      description="Stock count, discrepancy tracking, expiry alerts, overstock alerts, low-stock alerts, and inventory health."
    />
  );
}
import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="System settings, brand units, categories, icons, alert thresholds, user preferences, and platform configuration."
    />
  );
}